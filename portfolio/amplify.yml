version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd portfolio
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: portfolio/.next
    files:
      - '**/*'
  cache:
    paths:
      - portfolio/.next/cache/**/*
      - portfolio/node_modules/**/*
